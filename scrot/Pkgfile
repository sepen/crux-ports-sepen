# Description: Command line screen capture utility
# URL: https://github.com/resurrecting-open-source-projects/scrot
# Arch Maintainer: Jose Beneyto, sepen at crux-arm dot nu
# Depends on: imlib2 libbsd xorg-libxcomposite xorg-libext xorg-libxfixes xorg-libxinerama

name=scrot
version=1.12.1
release=1
source=(https://github.com/resurrecting-open-source-projects/$name/releases/download/$version/$name-$version.tar.bz2)

build() {
  cd $name-$version

  ./configure --prefix=/usr

  make
  make DESTDIR=$PKG install

  rm -rf $PKG/usr/share/doc
}
